{
    "name": "<%= package %>",
    "version": "1.0.0",
    "license": "MIT",
    "scripts": {
      "start": "parcel src/index.html",
      "build": "parcel build src/index.html"<% if (docker) { %>,
      "docker": "docker build -t <%= docker %> .",
      "docker:push": "docker push <%= docker %>"<% } -%>
    }
  }
  